{
  "name": "niite",
  "version": "1.0.0",
  "description": "NIITE — pata fundi wa kila aina, haraka.",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "cap sync android",
    "add:android": "cap add android",
    "build:apk": "cap sync android && cd android && chmod +x gradlew && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
